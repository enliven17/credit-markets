/* eslint-disable @typescript-eslint/no-explicit-any */`nimport { useState, useEffect, useCallback } from 'react';`nimport { leaderboardService, LeaderboardUser, LeaderboardTimeframe, LeaderboardCategory } from '@/lib/leaderboard-service';`n`nexport function useLeaderboard() {`n  const [users, setUsers] = useState<LeaderboardUser[]>([]);`n  const [loading, setLoading] = useState(true);`n  const [error, setError] = useState<string | null>(null);`n  const [timeframe, setTimeframe] = useState<LeaderboardTimeframe>('all-time');`n  const [category, setCategory] = useState<LeaderboardCategory>('total-points');`n`n  const fetchLeaderboard = useCallback(async () => {`n    try {`n      setLoading(true);`n      setError(null);`n      `n      const data = await leaderboardService.getLeaderboard(timeframe, category, 100);`n      setUsers(data);`n    } catch (err: any) {`n      setError(err.message || 'Failed to load leaderboard');`n    } finally {`n      setLoading(false);`n    }`n  }, [timeframe, category]);`n`n  useEffect(() => {`n    fetchLeaderboard();`n  }, [fetchLeaderboard]);`n`n  const getUserRank = useCallback(async (userAddress: string): Promise<number> => {`n    return await leaderboardService.getUserRank(userAddress);`n  }, []);`n`n  return {`n    users,`n    loading,`n    error,`n    timeframe,`n    setTimeframe,`n    category,`n    setCategory,`n    fetchLeaderboard,`n    getUserRank,`n    refresh: fetchLeaderboard`n  };`n}